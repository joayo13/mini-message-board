<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
     <% for(let i =0; i < messages.length; i++) { %>
      <div><%= messages[i].text + ' - ' + messages[i].user + ' - ' + messages[i].added %></div>
      <% } %>
    </ul>
  </body>
</html>
